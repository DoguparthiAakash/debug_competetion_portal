# ============================
# CONTEST PLATFORM - ENV CONFIG
# ============================

# MongoDB Connection
MONGO_URI=mongodb://localhost:27017/debugcontest

# Server
PORT=5000
NODE_ENV=production

# Admin Credentials (CHANGE THESE BEFORE DEPLOYMENT!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=vsbadmin

# JWT Secret (CHANGE to a random secret in production!)
JWT_SECRET=debug_contest_jwt_secret_key_2024

# Docker Settings (Adjust based on your system RAM)
# For 8GB RAM: keep defaults
# For 16GB+ RAM: can increase to DOCKER_MEMORY_LIMIT=512m and DOCKER_CPU_LIMIT=1.0
DOCKER_TIMEOUT_SECONDS=15
DOCKER_MEMORY_LIMIT=256m
DOCKER_CPU_LIMIT=0.5

# Frontend URL (for CORS)
# For local network deployment, replace with your local IP
# Example: CLIENT_URL=http://192.168.1.100:3000
CLIENT_URL=http://localhost:3000

# Performance Settings (for 200 concurrent users)
# MongoDB connection pool is configured in backend/config/database.js
# Docker execution queue is configured in backend/services/executionQueue.js
